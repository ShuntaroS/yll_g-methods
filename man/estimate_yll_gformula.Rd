% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{estimate_yll_gformula}
\alias{estimate_yll_gformula}
\title{Estimate YLL via the g-formula (ATE / ATT / ATC)}
\usage{
estimate_yll_gformula(
  data,
  B = 1000,
  seed = 1,
  conf_level = 0.95,
  method = c("normal", "percentile"),
  show_progress = TRUE,
  id_var = "id",
  time_var,
  event_var,
  exposure_var,
  reference_level = NULL,
  exposed_level = NULL,
  age_at_entry_var,
  age_start = 50,
  age_end = 100,
  age_interval = 5,
  confounders_baseline = NULL,
  estimand = c("ATT", "ATC", "ATE"),
  integration = c("left_rectangle", "trapezoidal"),
  use_future = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} with one row per individual, containing the
variables specified by \code{id_var}, \code{time_var}, \code{event_var}, \code{exposure_var},
\code{age_at_entry_var}, and any \code{confounders_baseline}.}

\item{B}{Number of bootstrap iterations. Set to \code{0} to skip bootstrapping
(returns point estimates only).}

\item{seed}{Integer seed for reproducibility.}

\item{conf_level}{Confidence level for the intervals (e.g. \code{0.95}).}

\item{method}{Either \code{"normal"} (Wald-type interval using bootstrap SE) or
\code{"percentile"} (bootstrap percentile interval).}

\item{show_progress}{Show a progress bar during bootstrap.}

\item{id_var, time_var, event_var, exposure_var}{Column names in \code{data}.}

\item{reference_level, exposed_level}{Values labelling the two exposure
levels. If \code{NULL} they are inferred from the data.}

\item{age_at_entry_var}{Column name for age at study entry.}

\item{age_start, age_end, age_interval}{Starting ages for which YLL is
reported, given as \code{seq(age_start, age_end, age_interval)}.}

\item{confounders_baseline}{Character vector of baseline confounder column
names. Including the entry-age column here is discouraged and triggers a
warning, since attained age is already modelled via splines.}

\item{estimand}{One of \code{"ATT"}, \code{"ATC"}, or \code{"ATE"}.}

\item{integration}{Discretisation rule for the area under the conditional
survival curve: \code{"left_rectangle"} (default; matches the discrete-time
hazard model) or \code{"trapezoidal"}.}

\item{use_future}{If \code{TRUE}, parallelise the bootstrap with
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}. The caller is expected to set a parallel
plan (e.g. \code{future::plan(future::multisession())}).}
}
\value{
A list with elements
\describe{
\item{\code{summary}}{Point estimates and CI for YLL at each starting age.}
\item{\code{detailed_results}}{Same as \code{summary} plus LE under each arm and
all CI / SE columns.}
\item{\code{meta}}{Run settings (\code{B}, \code{seed}, \code{conf_level}, \code{method},
\code{estimand}, \code{age_start}, \code{age_end}, \code{age_interval}, \code{integration}).}
\item{\code{marginal_survival_point}}{Population-marginal counterfactual
survival curves (point estimate).}
\item{\code{marginal_survival_boot}}{Same curves across bootstrap
iterations (used by the plotting helpers).}
}
}
\description{
Fits a discrete-time hazard model on the attained-age time scale, builds
counterfactual survival curves under "all reference" vs. "all exposed"
interventions, and reports Years of Life Lost (YLL) and counterfactual life
expectancy (LE) at user-chosen starting ages, with bootstrap confidence
intervals.
}
\details{
This function is the main legacy interface. For arbitrary interventions or
target populations, use \code{\link[=estimate_yll_gformula_intervention]{estimate_yll_gformula_intervention()}}.
}
\examples{
\donttest{
data(yll_toy, package = "YLLgmethods")
res <- estimate_yll_gformula_ate(
  data                 = yll_toy,
  B                    = 0,
  show_progress        = FALSE,
  time_var             = "period",
  event_var            = "event",
  exposure_var         = "hypertension",
  reference_level      = "No",
  exposed_level        = "Yes",
  age_at_entry_var     = "age",
  age_start            = 50,
  age_end              = 80,
  age_interval         = 10,
  confounders_baseline = c("sex", "education_years", "bmi", "smoke_binary"),
  use_future           = FALSE
)
res$summary
}

}
\seealso{
\code{\link[=estimate_yll_gformula_intervention]{estimate_yll_gformula_intervention()}},
\code{\link[=estimate_yll_gformula_binary_stochastic_vs_natural]{estimate_yll_gformula_binary_stochastic_vs_natural()}}, \code{\link[=plot_yll]{plot_yll()}}.
}
